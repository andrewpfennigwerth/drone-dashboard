# Copy this file to .env.local and paste a free Cesium ion token.
# Get one at https://cesium.com/ion -> Access Tokens. .env.local is gitignored,
# so your token never enters the repo. Only VITE_-prefixed vars reach the client.
VITE_CESIUM_ION_TOKEN=
