"""gcs -- the read-only ground control station application package.

Holds the telemetry source seam, the VehicleState model, the pure reducer, the
system-ID-keyed VehicleStore, and the console runner. See CLAUDE.md and
v1plan.md at the repo root for the architecture and invariants.
"""
