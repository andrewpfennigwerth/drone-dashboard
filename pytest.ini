[pytest]
# test_connection.py is the M0 telemetry probe, not a test: it opens a UDP
# socket and blocks on wait_heartbeat() at import time, which would hang
# pytest's collection. Keep it out of the test run.
addopts = --ignore=test_connection.py
