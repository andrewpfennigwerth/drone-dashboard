[pytest]
# Code lives in the gcs/ package; put the repo root on sys.path so tests can
# import it as `from gcs... import ...`. testpaths keeps collection to tests/.
# (The M0 probe now lives in tools/ and no longer matches test_*, so pytest
# won't collect it -- no --ignore needed.)
pythonpath = .
testpaths = tests
