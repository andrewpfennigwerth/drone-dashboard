# Backend dependencies for the drone telemetry dashboard.
# Set up a fresh environment with:
#   python3 -m venv .venv
#   .venv/bin/python -m pip install -r requirements.txt
pymavlink==2.4.49
pytest==9.0.3

# M2 backend: web server + WebSocket (uvicorn[standard] brings the websockets lib)
fastapi==0.138.0
uvicorn[standard]==0.49.0

# Test-only: FastAPI's TestClient is built on httpx (used by tests/test_server.py)
httpx==0.28.1
